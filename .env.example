# Copie para .env.local e preencha. O .env.local não vai para o repositório.

# Connection string do Neon (Dashboard > Connection Details).
DATABASE_URL=

# Senha única compartilhada. Se ficar vazia, o app abre sem pedir senha —
# vale para rodar local, não para publicar.
APP_SENHA=
